'use client';

/**
 * إعادة توجيه للصفحة الجديدة (V2)
 * تم استبدال الصفحة القديمة بالكامل بتوجيه للصفحة الجديدة المحدثة.
 * الكود المصدري موجود في ../users-v2/page.tsx
 */

import UsersPageV2 from '../users-v2/page';

export default function UsersPage() {
  return <UsersPageV2 />;
}
